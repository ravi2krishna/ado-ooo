kind: Service
apiVersion: v1
metadata:
  name: nginx-service
spec:
  ports:
    # Accept traffic sent to port 80
    - name: http
      port: 80
      nodePort: 30000
  selector:
    app: nginx
  type: NodePort